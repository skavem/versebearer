bin
.task
.env

go.sum

# test.db и search.bleve в рабочем каталоге — исторические остатки:
# программа больше не создаёт их здесь (данные переехали в
# %LOCALAPPDATA%\versebearer, см. backend/paths), но test.db* покрывает и
# test.db.migrated — след автоматического переноса старой базы при первом
# запуске новой версии на этой машине.
test.db*
search.bleve

.svelte-kit
frontend/src/lib/bindings
frontend/dist
node_modules
package-lock.json

.omc/
tmp/
songs.json
ROADMAP.md

# ffmpeg для установщика — сотня мегабайт, скачивается задачей fetch:ffmpeg
build/windows/ffmpeg/
